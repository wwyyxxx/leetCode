负责云顶之弈电视台,暗区突围,使命召唤电视台版本开发。
参与和平营地电视台,优量汇sdk,全民秒开赛等项目开发
支援和平精英,王者荣耀等电视台项目开发

VES全民秒开赛  2021-03-24——2021-05-14
    基于Androidx,采用mvvm架构,使用Retort、okhttp作为网络请求,Glide作为图片加载
    参与开发并上线v1.0版本。
    本人职责: 
        QQ/微信登录、实名验证、密钥验证、游戏角色绑定业务,版本升级等需求开发
        整理Activity,Fragment等页面基类,完成各页面停留上报
        通过LeakCanary分析并处理内存泄漏问题.
    下载安装:
        DownloadManager系统下载,广播监听下载完成,Intent调起安装(dataType). 注意文件路径的读取(fileProvider)
    onDraw死循环:
        setTextColor特定情况会调用invalidate
    Banner滑动冲突(RecyclerView):
        getParent().requestDisallowInterceptTouchEvent(true)
    自定义进度条:
        重写onDraw,使用PorterDuffXfermode设置混合模式绘制覆盖文字变色


腾讯广告——优量汇sdk  2021-01-11——2021-03-04
    为第三方app提供广告接入能力。
    基于mvc架构,使用Retort、okhttp作为网络请求框架,Glide图片加载。
    本人职责: 
        开发并上线v1.0、v1.1、v1.2三个版本
        负责开屏广告、激励广告、插屏广告等需求开发
        完成所有广告数据统计上报,以及推广app下载安装功能。
    缓存策略:
        先加载广告,再展示(判断广告是否存在、网络状态)
        路径写法:Environment.getExternalStorageDirectory().toString()
    自定义VideoView解决视频裁剪问题
        继承TextureView, MediaPlayer播放
        需求:视频等比例放大,直至一边铺满View的某一边,另一边超出View的另一边,再移动到View的正中央,这样长边两边会被裁剪掉同样大小的区域,视频看起来不会变形
        也即是:先把视频区(实际的大小显示区)与View(定义的大小)区的两个中心点重合, 然后等比例放大或缩小视频区,直至一条边与View的一条边相等,另一条边超过
        View的另一条边,这时再裁剪掉超出的边, 使视频区与View区大小一样. 这样在不同尺寸的手机上,视频看起来不会变形,只是水平或竖直方向的两端被裁剪了一些.
        --------------------------------------
        //第2步:把视频区移动到View区,使两者中心点重合.
        matrix.preTranslate((getResizedWidth() - videoWidth) / 2, (getResizedHeight() - videoHeight) / 2);
        //第1步:因为默认视频是fitXY的形式显示的,所以首先要缩放还原回来.
        matrix.preScale(videoWidth / (float) getResizedWidth(), videoHeight / (float) getResizedHeight());
        //第3步,等比例放大或缩小,直到视频区的一边超过View一边, 另一边与View的另一边相等. 因为超过的部分超出了View的范围,所以是不会显示的,相当于裁剪了.
        matrix.postScale(maxScale, maxScale, getResizedWidth() / 2, getResizedHeight() / 2);//后两个参数坐标是以整个View的坐标系以参考的
    下载安装:
        DownloadManager系统下载,广播监听下载完成,Intent调起安装(dataType). 注意文件路径的读取(fileProvider)
    图片模糊:
        RenderScript处理图片模糊(图像处理、计算摄影或计算机视觉的应用)



电视台sdk
    提供直播模块功能给王者荣耀,使命召唤,和平营地等项目接入。
    基于mvp架构,使用kotlin开发。
    负责云顶之弈电视台1.0版本,暗区突围电视台1.0版本,使命召唤5.0版本的需求开发及后续维护
    使用自定义View支援和平精英,王者荣耀v62等电视台项目的UI改造

    和平精英:       勋章升级优化
    王者荣耀:       UI3.0更新
    QQ飞车():       bug修复
    TK(暗夜突围): 2021/5/18 —— 2021/8/19
        v1.0 UI调整、手势控制、全面屏适配、反馈需求、深色模式
    JK(云顶之弈): bug处理、UI还原
    CODM(使命召唤): 2021/6/3 —— 2021/9/16
        v5.0、v6.0版本开发
        拍脸浮层功能、、QQ/微信分享功能开发
        枪匠功能:
            自定义view:
                included标签使用.
                自定义描边TextView:内部初始化一个TextView,只绘制边框
                    重写onMeasure(给予宽高)、onLayout(布局)、onDraw(绘制)、setLayoutParams()
        //全面屏适配: setNotchHeight(NotchStatusBarUtils.getStatusBarHeight(this));
        点播原生化:
            手势滑动控制亮度音量(GestureDetector):
                重写View的onTuch函数,给GestureDetector处理,return true. (如果是Activity dispatchTouchEvent)
            弹幕库:
        QQ/微信分享:
            MSDK接入
        进房引导条功能:
            TranslateAnimation、AlphaAnimation

和平营地电视台sdk: 2021-09-15
    基于Androidx,采用mvvm架构,使用Retort、okhttp作为网络请求,Glide作为图片加载
    v17.5 v17.8 v18
    功能中心、每日任务、红包、横屏场景
    内存泄露:
        1.webView.destory()
        2.Handler.removeCallbacksAndMessages
        3.array.clear()
        4.Fragment中ViewModel用viewLifecycleOwner监听 onDestroyView
    Lifecycle:空白Fragment 状态机
    LiveData:
        post(子线程)->setValue(主线程)
        遍历observer...判断状态机active、version
        不停的循环遍历直到有监听绑定
    ViewModel:

熟悉直播和即时通讯等业务开发,参与多个腾讯系项目。
熟练使用Java以及kotlin,H5拥有Cordova+ionic经验;
熟悉自定义控件以及多线程的使用;
对Android的UI、内存等性能优化有实践应用
了解Android下网络HTTP协议、socket协议编程;